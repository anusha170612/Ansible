---
 - hosts: localhost
   become: true
   tasks:
    - name: install httpd
      yum:
       name: httpd
       state: present
    - name: start httpd and enable
      service:
        name: httpd
        state: started
        enabled: true
